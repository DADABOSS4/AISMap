__pycache__/
*.pyc
.venv/
venv/

# Données générées / secrets — ne doivent jamais être commités,
# et ne vivent de toute façon jamais dans ce dépôt en déploiement
# (voir deploy/README.md : tout ça va dans ~/aismap-data, hors du repo)
*.jsonl
mmsi_imo_cache.json
targets.csv
stream/
*.env

# Sorties de carte générées en local (voir README.md) — jamais committées, la commande
# par défaut les écrit à la racine du dépôt cloné.
heatmap.png
heatmap.html
grid.npz
out/
